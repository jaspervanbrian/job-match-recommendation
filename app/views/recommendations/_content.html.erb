<div class="mt-4 relative overflow-x-auto shadow-md sm:rounded-lg">
    <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
        <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
            <tr>
                <th scope="col" class="px-6 py-3">
                    Job Seeker ID
                </th>
                <th scope="col" class="px-6 py-3">
                    Name
                </th>
                <th scope="col" class="px-6 py-3">
                    Job ID
                </th>
                <th scope="col" class="px-6 py-3">
                    Job Title
                </th>
                <th scope="col" class="px-6 py-3">
                    Matching Skills
                </th>
                <th scope="col" class="px-6 py-3">
                    Matching Skills Percentage
                </th>
            </tr>
        </thead>
        <tbody id="recommendations_table">
            <%# <%= render partial: 'recommendation', collection: @recommendations, cached: true %1> %>

            <% @recommendations.each do |recommendation| %>
                <%= render 'recommendation', recommendation: %>
            <% end %>
        </tbody>
    </table>
</div>

<%= render "load_more", load_more_path: root_path(page: @page + 1, format: :turbo_stream) %>
